import styled from "styled-components";

export const StyledFooter = styled.footer`
    display: flex;
    justify-content: center;

    .footer__link {
        color: rgba(0, 0, 0, .7);
        padding-left: 0;
        display: block;
    }
`